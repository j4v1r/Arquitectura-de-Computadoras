ATMEGA328P

	.def temp=r16
	.def cte=r17
	.def cte2=r18
	.def cont1=r19
	.def cont2=r20
	.def cont3=r21
	.cseg
	.org 0

	nop
	nop
	ldi cte,$01
	;ldi temp,$ff
	;out ddrd,temp
	;ldi cte2,$01
	;cambiar portb->portd

izquierda:ldi temp,$01
	ldi cte,$80
	out portb,temp ;encendemos led B0
	rcall delay_125m
izquierda_2:lsl temp
	out portb,temp
	rcall delay_125m
	sub temp,cte
	breq derecha
	add temp, cte
	rjmp izquierda_2

derecha:ldi temp,$80
	ldi cte,$01
derecha_2:lsr temp
	out portb,temp
	rcall delay_125m
	sub temp,cte
	breq izquierda
	add temp,cte
	rjmp derecha_2
	
delay_125m:ldi cont1,31
;cont1,5

;avrdude.exe
;Toolos microchip, external tools, pegar en linea donde dice comando digoanles invertidas

;avrdude.conf 3entre comilas
;Uflash:w:$(TargetDir)$(TargetName).hex:i
;pegar en donde cice arguments

;use outout window
lazo3:	ldi cont2,200
lazo2:	ldi cont3,200 
lazo1:	nop
	nop
	nop
	nop
	nop
	nop
	nop
	nop
	sub cont3,cte2
	brne lazo1
	sub cont2,cte2
	brne lazo2
	sub cont1,cte2
	brne lazo3
	ret
